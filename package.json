{
  "name": "node-app",
  "version": "1.0.0",
  "description": "Node.js application for IBM Cloud deployment test",
  "main": "app.js",
  "scripts": {
    "start": "node app.js",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "dependencies": {
    "express": "^4.17.1"
  }
}
